{{ lowerfirst .name }}NS := apiV1.Group("/{{ .name_underline }}")
{
    {{ lowerfirst .name }}NS.POST("/index", {{ .package_name }}.{{ .name }}Index4Op)
    {{ lowerfirst .name }}NS.POST("/create", {{ .package_name }}.{{ .name }}Create4Op)
    {{ lowerfirst .name }}NS.POST("/update", {{ .package_name }}.{{ .name }}Update4Op)
    {{ lowerfirst .name }}NS.POST("/delete", {{ .package_name }}.{{ .name }}Delete4Op)
}
